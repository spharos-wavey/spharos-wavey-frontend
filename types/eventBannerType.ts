export interface eventBannerType {
  carId: number,
  carName: string,
  carBrand: string,
  carBrandImage: string,
  carImage: string,
}

export interface mainVehicleCardType {
  id: number,
  carName: string,
  carBrand: string,
  carImage: string,
  pickUpArea: string
}